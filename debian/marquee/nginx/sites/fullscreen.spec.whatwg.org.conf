server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name fullscreen.spec.whatwg.org;
    root /var/www/fullscreen.spec.whatwg.org;

    ssl_certificate /etc/letsencrypt/live/fullscreen.spec.whatwg.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/fullscreen.spec.whatwg.org/privkey.pem;

    include /etc/nginx/whatwg.conf;
}
