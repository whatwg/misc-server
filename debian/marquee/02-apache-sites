#!/bin/bash -e

# a2dissite doesn't work if nothing is enabled
rm -f /etc/apache2/sites-enabled/*
cp apache/sites/*.conf /etc/apache2/sites-available/

for domain in `cat DOMAINS | tr , ' '`; do
    a2ensite $domain || :
done

apache2ctl configtest
apache2ctl restart
